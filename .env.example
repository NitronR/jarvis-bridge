# Jarvis Bridge — Environment Configuration
#
# Copy to .env and fill in. Lines beginning with # are comments.
# All names below are documented; defaults shown are what the gateway uses
# when the variable is unset (or set to anything other than the listed opt-in).

# ── Core ──────────────────────────────────────────────────────────────────
# Workspace root (created if missing). The agent's default cwd.
JARVIS_BRIDGE_WORKSPACE=~/.jarvis-bridge

# Gateway HTTP port.
PORT=3001

# ── Agent subprocess ─────────────────────────────────────────────────────
# The agent CLI binary. Use an absolute path if not on PATH.
AGENT_CMD=

# Whitespace-separated args to start the ACP agent over stdio.
AGENT_ARGS=

# Optional model pin applied to NEW sessions via session/set_model.
AGENT_MODEL=

# Backend-wide default for the Auto-approve toggle.
# Opt-in: only the literal string "true" enables; anything else is false.
AGENT_AUTO_APPROVE=false

# ── Context injection ────────────────────────────────────────────────────
# Enable context injection. Anything but "false" enables.
INJECT_CONTEXT=true

# "paths" (token-light catalog) or "full" (inline file contents).
INJECT_CONTEXT_MODE=paths

# ── Workspace bootstrap ──────────────────────────────────────────────────
# Template source directory copied into a fresh workspace on first run.
INITIAL_WORKSPACE_PATH=./initial_workspace

# Comma-separated skill names to install on first run.
JARVIS_BRIDGE_INITIAL_SKILLS=

# Opt into the first-run onboarding conversation.
JARVIS_BRIDGE_ONBOARDING=false

# ── Terminal drawer ──────────────────────────────────────────────────────
# Set false to disable the WebSocket terminal drawer entirely.
JARVIS_BRIDGE_SHELL=true

# ── Optional integrations ────────────────────────────────────────────────
# Slack bot token (xoxb-...) for POST /slack/message.
SLACK_BOT_TOKEN=

# Gateway URL used by the optional stdio MCP server (P6).
JARVIS_BRIDGE_GATEWAY_URL=http://localhost:3001